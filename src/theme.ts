import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    // text: {
    //   primary: '#4361ee',
    //   disabled: '#4361ee',
    // },
  },
  typography: {
    button: {
      textTransform: "none",
      fontWeight: 400,
    },
  },
});
